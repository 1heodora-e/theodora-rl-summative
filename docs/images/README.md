# Dashboard screenshot (`viz-dashboard.png`)

This folder holds the **README hero image** for the Three.js + FastAPI distribution dashboard.

- **File:** `viz-dashboard.png` — commit this so GitHub renders the preview at the top of `README.md`.
- **Update:** Replace `viz-dashboard.png` when the UI changes; keep the same filename.
